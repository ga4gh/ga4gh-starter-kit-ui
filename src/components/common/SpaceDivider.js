import React from 'react';
import { 
    Box,
    Divider
} from '@material-ui/core';

const SpaceDivider = () => {
    return (
        <Box py={4}>
            <Divider />
        </Box>
    );
}

export default SpaceDivider;
