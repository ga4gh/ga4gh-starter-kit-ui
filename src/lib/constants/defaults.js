const defaults = {
    port: 8989,
    services: [
        {
            serviceType: 'drs',
            publicURL: 'http://localhost:4500',
            adminURL: 'http://localhost:8080'
        }        
    ]
};

export default defaults;
