import { makeStyles } from '@material-ui/core/styles';

const viewButtonStyles = makeStyles((theme) => ({
    root: {
        marginLeft: 10,
        marginRight: 10
    }
}));

export default viewButtonStyles;
