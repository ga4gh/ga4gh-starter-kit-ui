import { makeStyles } from '@material-ui/core/styles';

const drsObjectIndexStyles = makeStyles((theme) => ({
    newButton: {
        marginBottom: 25
    }
}));

export default drsObjectIndexStyles;
